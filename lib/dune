(library
 (name goblin)
 (libraries unix menhirLib str re cmdliner ocamlgraph bitstring yojson lwt lwt.unix batteries)
 (preprocess (pps ppx_bitstring lwt_ppx))
 )

(menhir
 (modules parser solverParser))

(ocamllex lexer solverLexer)

(include_subdirs unqualified)
